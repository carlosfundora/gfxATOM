# Changelog

All notable crate-specific changes for `rs_kv_codec_adapters` are recorded here.

## [Unreleased]

### Added

- Added crate-local documentation coverage with `README.md` and `CHANGELOG.md` tracking for `rs_kv_codec_adapters` (0.1.0).
